{
  "name": "@css-atlas/shared",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npx --prefix ../backend vitest run --environment node time.test.ts"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
